import React from 'react';
import ReactDOM from 'react-dom';
import AppComponent from './app';

const title = 'Video Tags Marker';

ReactDOM.render(<AppComponent />, document.getElementById('app'));

module.hot.accept();
